v0.7.2 更新说明（与 v0.7.1 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

## ✨ 新增

**源码更新可选自动执行 `pnpm run build`**

更新器做的是"整目录替换"，只保留 `node_modules` 与 `.git`。而构建产物
（`apps/cli/lib` 等）既不在官方源码 zip 里、也不在保留列表里，**替换后必然丢失**。
用源码目录启动 DSH 的用户必须重新构建，否则会以 `client bundles not found`
之类的错误启动失败。

原先更新器只提供 `pnpm install` 的勾选框——勾了、更新完、启动失败，用户完全
不知道还差哪一步。现在更新确认框有两个勾选框：

| 勾选框 | 说明 |
|---|---|
| 替换后自动运行 pnpm install | 同步依赖（推荐） |
| 替换后自动运行 pnpm run build | 源码版启动必需（默认不勾，构建较耗时） |

**源码更新后提示需要重新构建**

即使不勾自动构建，替换完成后检测到"原目录有 `apps/cli/lib`、新目录没有"时，
也会在日志和完成提示里明确告知：

> 注意：原目录中的构建产物（apps/cli/lib）未随源码保留。
> 若你用它直接启动 DSH，请重新执行：pnpm install && pnpm run build

## 🔧 修复

**日志区长消息的多语言覆盖**

折行拼接的长消息（例如 `f"已下载 {a:.1f} MB" f"（{b:.0f}%）｜剩余 {c}"`）此前
未接入多语言，日志区仍是中文。现在这些消息已整体转为可翻译文本。

**两条重要提示的翻译键失效**

「请先关闭 DSH 再更新 npm 全局」与「构建产物未保留，请重新构建」这两条提示，
由于翻译键里的换行被误写成字面量反斜杠+n，与源码不匹配而**静默回退中文**。
现已修正。

## ⚙️ 工程改进

- 翻译键改由脚本从源码提取（ast 解析真实字符串），不再手工重建，从根上避免转义错误
- 新增严格校验测试：遍历源码中所有 `t("...")` 字面量键，逐一确认四种语言表里都存在
- 测试 117 → 121 项

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.7.2.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。

## ⚠️ 已知限制

- 切换语言后若个别文字仍显示为其它语言，点一次「🔄 重新检测」刷新即可
- 「使用 GPU 加速」目前仅记录偏好：DSH 尚未提供 GPU 加速选项
