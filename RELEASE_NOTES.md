v0.6.8 更新说明（与 v0.6.7 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

## ✨ 新增功能

**检查更新器自身更新**

- 主窗口右下角显示当前版本号，点击即可弹出小窗口检测是否有新版本
- 按「发布时间」挑选最新发布，而不是按版本号——本仓库历史 tag 存在版本号倒挂
  （`v1.0.1` 指向的提交比 `v0.6.7` 更早），按版本号挑会误报「有新版本」
- 有新版时提供「打开 GitHub 页面」按钮；检测失败会显示具体原因而不是静默无反应

**偏好设置**

- 主界面新增「偏好设置」框，可勾选「使用 GPU 加速」
- ⚠️ 说明：DSH 目前未提供 GPU 加速选项（源码与 profile 配置中均无相关键），
  此选择**仅记录在本地**，暂不影响任何行为，界面上已明确标注
- 设置保存于 `%APPDATA%\dsh-updater\settings.json`，重启后自动恢复；
  文件损坏、格式不符或类型错误时会安全回退默认值，不影响启动

## 🔧 修复

**CI 从长期红灯恢复为全绿**

- 原 `python-package-conda.yml` 是未适配本项目的 GitHub 模板，执行
  `conda env update --file environment.yml`，但仓库中并不存在 `environment.yml`，
  导致**每次推送都必然失败**（约 24 秒报错）
- 移除 `python-publish.yml`：它触发于 Release 创建，执行 `python -m build`
  （仓库无 `pyproject.toml` / `setup.py`），且面向 PyPI，并非本项目分发渠道

**修复底部版本号不显示**

- 底部状态栏原先在 pack 顺序里排最后，内容总高超出窗口时会被**完全挤掉**
  （实测 `winfo_ismapped()` 返回 0，即根本没显示）
- 调整窗口尺寸：默认 `1020x700`、最小 `860x660`，安装列表 6→5 行、日志区 9→4 行，
  保证状态栏与日志区在最小尺寸下均可见

**修正更新检测的提示措辞**

- 远端最近发布版本低于本地时，原文案暗示「可能是历史 tag 倒挂」，
  但最常见的原因其实是**本地版本尚未发布到 GitHub**（正常状态），已改为中性表述

## ⚙️ 工程改进

- 新增 CI 流水线 `ci.yml`：ubuntu + windows × Python 3.10 / 3.12，共 4 个作业，
  执行致命级静态检查与冒烟测试
- 静态检查显式纳入 `updater_gui.pyw`（flake8 默认只扫 `*.py`，
  界面文件此前**从未被任何检查覆盖**）
- 新增 `tests/` 共 65 项冒烟测试，不联网、不触碰真实用户目录，约 0.2 秒跑完

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.6.8.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本 v0.6.7 用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。
