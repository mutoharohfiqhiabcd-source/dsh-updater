v0.7.7 更新说明（与 v0.7.6 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

本版是深色模式的观感与可用性修复。

## ✨ 新增

**深色模式会被记住**

此前主题根本没有持久化：`_dark` 在启动时恒为 `False`，切换后也只改内存、
从不保存——所以每次打开程序都得手动再切一次深色。

现已记住选择，下次启动直接沿用。

## 🔧 修复

**回滚窗口在深色模式下整体改为纯黑**

这个独立的小窗口原先跟随主窗口的深蓝灰底色，层次不清晰。
现在深色模式下窗口、列表、滚动条、原生标题栏一律纯黑，与主窗口区分开。
浅色模式维持原样。

实现上用本窗口专属的样式名（`Rb.*`），**不改全局样式**，主窗口不受影响。

**回滚窗口提示文案后面的黑色矩形**

「勾选要回滚的项…」这句话后面曾有一块突兀的黑底。根因不是配色，而是
`style="Hint.TLabel"` 这个样式**被使用却从未定义**——ttk 拿到未配置的
样式名不会回退到 `TLabel`，而是渲染成黑底。

**深色模式下的其它可读性问题**

| 位置 | 问题 |
|---|---|
| 「语言」下拉框 | `_setup_style` 从未配置 `TCombobox`，clam 主题给出白底黑字，深色下是空白白框 |
| 插件 / 技能窗口横幅 | 背景写死浅色（`#eef7f0` 等），深色下刺眼 |
| 按钮按下态 | 按下色写死浅色 |

## ⚙️ 工程改进

- 新增测试 `test_all_used_ttk_styles_are_defined`：自动比对「源码里使用的 ttk
  样式」与「`_setup_style` 里定义的样式」，这类漏定义不会再溜过去
- 新增测试覆盖「使用的样式必须有定义」；测试 130 → 131 项

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.7.7.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。

## ⚠️ 已知限制

- 回滚窗口最旧的一个备份若读不出 package.json 的版本，会显示「回到 备份版本」
- 下拉框展开后的列表使用 Tk option 数据库上色，未能在开发机上回读验证
- 切换语言后若个别文字仍显示为其它语言，点一次「🔄 重新检测」刷新即可
- 「使用 GPU 加速」目前仅记录偏好：DSH 尚未提供 GPU 加速选项
