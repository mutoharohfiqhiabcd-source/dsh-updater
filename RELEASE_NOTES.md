v0.7.5 更新说明（与 v0.7.2 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

## ✨ 新增

### 更新前可自动关闭正在运行的 DSH

此前 DSH 开着时更新是死路：源码更新被端口检查拦住并报错；npm 更新则以
`EPERM` 中断，还可能留下**缺少子模块文件的损坏安装树**。

现在检测到 3080 端口被占用时会弹窗列出**将要结束的进程**（PID + 进程名），
你确认后才关闭，然后继续更新。取消则中止并记日志——不会偷偷杀进程。

### 三路回滚：源码检出 / npm 全局 / 运行时 profile

工具栏新增「↩ 回滚」按钮，打开回滚窗口：

| 类型 | 回滚方式 |
|---|---|
| 源码检出 | 先把当前目录另存为新备份，再换回所选备份（中途失败会自动还原，回滚本身可逆） |
| npm 全局 | 重装历史记录的旧版本（该类型先前不产生备份，故记录版本历史） |
| 运行时 profile | 本工具不单独更新它（由源码 / CLI 安装提供），界面会说明原因 |

跨类别可多选：只勾一项就是单独回滚，勾多项就是一起回滚。
每次更新成功后都会记入 `preferences.json` 的 `history` 段。

### 插件与技能合并为一个窗口

原先各开一个窗口、容易互相遮挡。现在合成一个窗口，用分页（Notebook）区分：
插件页保持暗绿强调色，技能页保持暗紫强调色；打开时两页都会构建，切换即时。

## 🔧 修复

- **主界面列被截断**：「状态」列显示成「官方版本获取失」、「类型」列显示成
  「运行时 profile (he」。根因是只有「位置」列参与伸缩，它吃掉了全部多余宽度。
  现按**字体实测**重定各列宽，并让位置与状态都参与伸缩
- **列表下方大片留白**：列表高度改为跟随实际条目数（3~8 行）
- **回滚窗口**：固定高度导致下方约 40% 空白；「时间」列在旧备份上整列为空；
  「位置」列把同一路径重复 8 遍。现按条目数定高，时间并入首列（可读格式），
  路径只在类别行出现一次

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.7.5.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。

## ⚠️ 已知限制

- 切换语言后若个别文字仍显示为其它语言，点一次「🔄 重新检测」刷新即可
- 「使用 GPU 加速」目前仅记录偏好：DSH 尚未提供 GPU 加速选项
- 回滚只覆盖本工具记录过的更新；早于本版的历史备份仍会列出，但版本号
  从备份目录的 package.json 读取，读不到时显示为「备份版本」
