v0.7.6 更新说明（与 v0.7.5 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

本版是深色模式的可读性修复。

## 🔧 修复

**深色模式下「语言」下拉框是空白白框、文字完全看不见**

根因：`_setup_style` 里完全没有配置 `TCombobox`。ttk 的 `clam` 主题不跟随
本程序的配色，默认给出白底黑字——切到深色后就是一个刺眼的白框。

现已显式配置字段底色、箭头、边框与选中色，并用 Tk option 数据库给
**展开后的原生 Listbox** 上色（ttk 样式管不到它）。

**插件 / 技能窗口的横幅背景在深色下刺眼**

`#eef7f0` / `#fff4e6` / `#fdeeec` 是写死的浅色。现改为读主题里的
`banner_bg`，深色下用 `#17301f` / `#3a2f16` / `#3a1c1c`。
进度异常的横幅同样处理。

**按钮按下色随主题**

原先写死 `#c8d2e0`（浅色），深色下按下会有突兀的亮块。

## 📋 说明

- 列表行的背景色**未改动**。经核查它本来就走 `CLR["row_bg"]`，
  深浅两套取值都正确，不是缺陷。
- 本版没有功能改动，仅配色与可读性。

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.7.6.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。

## ⚠️ 已知限制

- 下拉框**展开后的列表**使用 Tk option 数据库上色，未能在开发机上回读验证；
  若深色下展开仍是白底，请反馈
- 切换语言后若个别文字仍显示为其它语言，点一次「🔄 重新检测」刷新即可
- 「使用 GPU 加速」目前仅记录偏好：DSH 尚未提供 GPU 加速选项
