v0.7.0 更新说明（与 v0.6.8 相比）

> 本文件是「最新一版」的更新说明，发布新版本前请改成当版内容。
> GitHub Actions 在推送 `v*` tag 时会把这个文件的内容作为 Release 说明（`body_path`）。

## ✨ 新增功能

**多语言支持（5 种语言）**

- 首次启动自动检测系统语言并切换界面语言，也可在「偏好设置」里手动指定
- 支持：简体中文 / 繁體中文 / English / 日本語 / 한국어
- 切换后界面立即重建，无需重启；选择会被记住（`settings.json` 的 `language`
  字段，默认 `auto` 跟随系统）
- 繁体／简体按 Windows SUBLANG 正确区分（大陆简体、台湾/香港/澳门繁体、新加坡简体）
- 采用「以简体中文原文为键」的方案，缺翻译自动回退原文，**界面不会出现空白**

**检测结果缓存**

- 状态栏版本号左侧显示「上次检测：YYYY-MM-DD HH:MM」
- 启动时先用上次的检测结果填充列表，再在后台重新检测，打开瞬间不再是空白
- 缓存损坏或结构不对时自动回退到「无缓存」路径，不影响启动

**插件 / 技能的下载来源**

- 插件窗口与技能窗口新增「🔗 打开下载来源」按钮
- 插件 → npm 包页；技能 → 其 git 来源仓库（`git@host:path` 会自动转成 https）
- 打开后把来源记入 `sources.json`，日后可据此检测更新
- 无法识别来源时给出明确提示，不会静默无反应

## 🔧 修复

- 修正偏好设置框与状态栏在深色模式下配色不跟随主题的问题
- 修正「检查更新」弹窗在远端版本低于本地时的措辞（原文案暗示 tag 异常，
  实际多因本地版本尚未发布）

## ⚙️ 工程改进

- 字符串提取与改写改用 `tokenize` 而非正则，避免误伤注释、docstring 与格式串
- 翻译数据与代码分离：`i18n_data.py` / `i18n_zh_tw.py` / `i18n_ja.py` /
  `i18n_ko.py` / `i18n_extra.py`，便于审阅与增量维护
- 测试从 69 项增至 112 项

## 📦 本版资产（两种版本）

| 文件 | 说明 |
|---|---|
| `DeepSeekHarnessUpdater.exe` | Windows 独立版（无需 Python），双击即用 |
| `dsh-updater-src-v0.7.0.zip` | Python 源码版：解压后双击 `启动更新器.bat`，或运行 `python updater_gui.pyw`（需 Python 3.10+，含 tkinter） |
| `SHA256.txt` | EXE 的 SHA256 校验值 |

上版本用户若已安装：直接覆盖 exe 即可，数据（CSV 导出）与本地设置不受影响。

## ⚠️ 已知限制

- 日志区中少量折行拼接的长消息尚未接入多语言，仍显示中文
- 切换语言后若个别文字仍显示为其它语言，点一次「🔄 重新检测」刷新即可；
  这不影响使用，也不代表语言设置没生效（仅影响少数在扫描时生成的日志/浮窗文案）
- 「使用 GPU 加速」目前仅记录偏好：DSH 尚未提供 GPU 加速选项，暂不影响任何行为
